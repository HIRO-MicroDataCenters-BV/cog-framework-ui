# Development environment variables
# API Base Path (relative path that ingress routes to API service)
# Default: /cogapi
NUXT_PUBLIC_API_BASE=/cogapi

# URL Prefix for deployment (leave empty for development)
URL_PREFIX=

# Enable mock data (set to 'true' to enable)
MOCK_ENABLED=false
