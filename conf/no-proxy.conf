server {
  listen 80;

  location /uidev/ {
    root /usr/share/nginx/html;
    index index.html index.htm;
    try_files $uri $uri/ /uidev/index.html =404;
  }

  location / {
    absolute_redirect off;
    return 301 /uidev/;
  }

  include /etc/nginx/extra-conf.d/*.conf;
}
